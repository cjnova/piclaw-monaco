services:
  piclaw-monaco:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    environment:
      PORT: ${PORT:-8080}
    ports:
      - "${PORT:-8080}:8080"
    volumes:
      - piclaw-data:/workspace
    restart: unless-stopped

volumes:
  piclaw-data:
